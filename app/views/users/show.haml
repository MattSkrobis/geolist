%h1= @user.to_s

%b Age
=@user.age
%br

-if @user.lists
  %table.table.table-hover
    %thead
      %th Lists
    %tbody
      -@user.lists.each do |list|
        %tr
          %td= list.name
          %td
            = link_to 'Show list', my_list_path(list)
            = link_to 'Change Name', edit_my_list_path(list)
%br

= link_to 'New list', new_my_list_path
= link_to 'Lists', lists_path
%br
